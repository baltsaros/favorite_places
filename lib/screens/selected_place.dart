import 'package:flutter/material.dart';

class SelectedPlace extends StatelessWidget {
  const SelectedPlace({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}